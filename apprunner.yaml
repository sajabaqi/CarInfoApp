version: 1.0
runtime: dotnet
build:
  commands:
    - dotnet restore CarInfoApiApp/CarInfoApiApp.csproj
    - dotnet publish CarInfoApiApp/CarInfoApiApp.csproj -c Release -o output
run:
  command: dotnet CarInfoApiApp.dll
  workingDirectory: /output
